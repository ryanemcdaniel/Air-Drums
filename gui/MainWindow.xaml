<Window x:Class="gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:gui"
        mc:Ignorable="d"
        Title="MIDI Selection" Height="600" Width="500">

  <StackPanel>
  <Grid Margin="10,80,10,10" >
  
  </Grid>
  <TextBox Name="gesture1" AcceptsReturn="True" TextWrapping="Wrap" Width = "200" /> 
  <TextBox Name="gesture2" AcceptsReturn="True" TextWrapping="Wrap" Width = "200" /> 
  <TextBox Name="gesture3" AcceptsReturn="True" TextWrapping="Wrap" Width = "200" /> 
  <TextBox Name="gesture4" AcceptsReturn="True" TextWrapping="Wrap" Width = "200" /> 
  <TextBox Name="gesture5" AcceptsReturn="True" TextWrapping="Wrap" Width = "200" /> 
  <TextBox Name="gesture6" AcceptsReturn="True" TextWrapping="Wrap" Width = "200" /> 
  <TextBox Name="gesture7" AcceptsReturn="True" TextWrapping="Wrap" Width = "200" /> 
  <TextBox Name="gesture8" AcceptsReturn="True" TextWrapping="Wrap" Width = "200" />
  <StackPanel Grid.Column="5" Grid.Row="0" HorizontalAlignment="Center" Name="ButtonPanel" VerticalAlignment="Top">
    <Button Margin="0,10,0,10">Dog Friendly</Button>

  </StackPanel> 
</StackPanel>
    
</Window>


