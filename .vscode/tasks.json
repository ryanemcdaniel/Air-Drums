{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "-o ${workspaceFolder}\\build",
                "${workspaceFolder}\\air-drums-project.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}